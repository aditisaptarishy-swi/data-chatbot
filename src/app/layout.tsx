import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'DataChat AI - Natural Language Data Analysis',
  description: 'Upload datasets and interact with them using natural language prompts. AI-powered SQL generation and data visualization.',
  keywords: 'data analysis, AI, SQL, natural language, CSV, Excel, charts, visualization',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.className} bg-gray-950 text-white antialiased`}>
        {children}
      </body>
    </html>
  )
}
